import {themeShema} from '../../../types/types';

const theme = (): themeShema => {
  return {
    bg: '#000',
    bg2: '#2c2c2c',
    text: '#fff',
    prime: '#00a0ab',
    second: '#e9e9e9',
    highlight: '#5a5a5a',
    yellow: '#fff6a5',
  };
};

export default theme;
