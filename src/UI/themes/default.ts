import {themeShema} from '../../../types/types';

const theme = (): themeShema => {
  return {
    bg: '#fff',
    bg2: '#fff',
    text: '#000',
    prime: '#00a0ab',
    second: '#8f8f8f',
    highlight: '#E9E9E9',
    yellow: '#fff6a5',
  };
};

export default theme;
